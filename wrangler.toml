# Cloudflare Pages 配置文件
# 用于纯静态项目部署
# 
# 注意：对于纯静态 Pages 项目，此文件是可选的
# 可以通过命令行参数直接配置：wrangler pages deploy dist --project-name=react-basic
#
# 如果需要使用此配置文件，请确保：
# 1. 不要包含 Workers 特定的配置（如 account_id、main 等）
# 2. 只包含 Pages 相关的配置（如 pages_build_output_dir）

pages_build_output_dir = "dist"

